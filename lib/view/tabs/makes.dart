import 'package:flutter/material.dart';

class MakesBody extends StatelessWidget {
  const MakesBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Alarm'),
    );
  }
}
